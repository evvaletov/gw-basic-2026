{"author":{"id":"f4f7e123ea38957901744390924e1710825e6d55b5113472173231f66fb2d926"},"ops":[{"type":3,"timestamp":1781347926,"nonce":"vyYCmYFTeXyWTYpmvtZ4d9XLwro=","message":"QA pass (task-qa, codex+agy) surfaced additional deferred items beyond the original two:\n- INSTR optional-start dispatch (codegen.c ~623-624) and WRITE string detection (codegen.c ~2545-2547) still use parse_var() and so misclassify a string-returning extern used as an INSTR/WRITE argument (degraded value — the call is still consumed correctly, not memory-unsafe). Same root cause as the emit_print fix; the clean fix is a shared 'identifier type incl. externs' helper used by all dispatchers.\n- POLISH: a type suffix on an extern call (e.g. Getn$()) is consumed without being validated against the declared return type.\n- POLISH: pragma name validation — reject digit-first names and names colliding with C reserved words (currently can emit invalid C).\n- POLISH: wrong-context type mismatch discards the call output (drops side effects); consider emitting a diagnostic instead of a silent placeholder.\n\nFixed in the same QA pass (NOT deferred): a use-after-free on string-return when the callee returns one of its char* args (copy-before-free), \u003e8-arg token desync (parse-all/store-capped + arity warning), and overlong name/type-word parse desync (drain loops).","files":null}]}